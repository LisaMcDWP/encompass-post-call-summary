steps:
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build', '-t', 'gcr.io/$PROJECT_ID/guideway-batch-job:latest', '-f', 'Dockerfile.batch', '.']
images:
  - 'gcr.io/$PROJECT_ID/guideway-batch-job:latest'
